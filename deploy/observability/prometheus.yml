global:
  scrape_interval: 15s
  evaluation_interval: 15s
  external_labels:
    service: grok2api

rule_files:
  - /etc/prometheus/alerts.yml

scrape_configs:
  - job_name: grok2api
    metrics_path: /
    static_configs:
      - targets:
          - grok2api:9090
