# 第 17 轮计划：v3.1.1 发布与最终归档

- 日期：2026-07-23
- 前置条件：第 16 轮同步 PR 已使用 Merge commit 合并，全部检查成功。
- 目标版本：`v3.1.1`

## 目标

1. 从最终 main 创建并只推送 `v3.1.1` 标签。
2. 发布稳定 GitHub Release，验证多架构 GHCR、provenance、SBOM 和健康检查。
3. 记录第 16、17 轮 PR、合并、Release 和工作流证据。
4. 完成最终文档归档 PR、分支清理和本地同步。

## 约束

- 标签必须与 `VERSION` 一致并指向 main 祖先。
- 不推送上游标签，不移动或覆盖 `v3.1.0`。
- Release 发布前必须确认没有 Critical/High 可达依赖漏洞。
- 原始日志、凭据、Trace、堆快照和临时数据库不得提交。

## 验收

- Release `v3.1.1` 成为最新稳定版本。
- GHCR 存在 `v3.1.1`、`3.1.1`、`3.1`、`3` 和 `latest`，双架构与 smoke 成功。
- 第 16、17 轮结果和计划索引均为完成。
- 本地 `HEAD == origin/main`，工作区干净，临时分支均删除。
