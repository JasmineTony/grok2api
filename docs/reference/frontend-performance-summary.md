# 脱敏前端性能摘要规范

性能报告只允许保存聚合值：提交哈希、页面、视口、浏览器、LCP、CLS、TTFB、INP 实验室代理、长任务总时长、bundle/chunk 数和堆大小汇总。原始 Trace、HAR、截图、Cookie、Authorization、请求正文和堆快照只能位于被忽略的 `.cache/`。

每次报告必须说明采样环境、是否 mock、是否有冷/热缓存以及与上一基线的差异；无法稳定复现的数值不得作为强制门禁。
