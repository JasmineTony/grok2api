# Protocol golden fixtures

These fixtures are offline inputs for JSON/SSE compatibility tests. Volatile IDs, timestamps and unordered JSON object keys are normalized before comparison. Update snapshots only with `UPDATE_GOLDEN=1`; tests never access the network.
